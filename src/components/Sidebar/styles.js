import {tv} from 'tailwind-variants'

export const playlist = tv({
  base: [
    'text-sm text-zinc-400 hover:text-zinc-100',
  ]
})

export const sideBarIcons = tv({
  base: [
    'flex items-center text-sm gap-3 font-semibold text-zinc-100'
  ]
})